Users.currecnt
Local.Username = (string);

String.apply

Function setuser() {
  if user.Username = false
  prompt("Set A Username")
  //textbox code
  If Confirm
   get string('' user); //textbox id
    User.setUser = string;
  if User.Username = true
    start.js
    destroy();
  
}
